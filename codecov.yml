coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        informational: true
    patch:
      default:
        target: auto
        threshold: 0%
        informational: true

# do not include tests folders
ignore:
  - "tests"
  - "proto"
  - "internal/codec/interface"
  - "internal/interface"
  - "internal/data_converter/converter_test.go"
